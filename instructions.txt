1. Initialize npm and install dependencies
 => npm i express, dotenv, cors, mongodb, ejs, nodemon --save-dev

2. Require dependencies

3. Create .gitignore file
    => add "node_modules" in .gitignore file

4. Declare variables

5. Connect to Mongodb, create .env file and add the connection string.
    - Setup actual connection method to the DB_STRING

6. Create Port
    - Test Mongodb and Port connection

7.